# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/_/settings/api
#
# NEXT_PUBLIC_SUPABASE_URL = Project URL
# NEXT_PUBLIC_SUPABASE_ANON_KEY = "anon public" key (this is the PUBLIC key, safe for browser)
# SUPABASE_SERVICE_ROLE_KEY = "service_role" key (PRIVATE - server-side only!)
#
# Note: Supabase calls it "anon key" but it's the same as the public key.
# The "service_role" key is needed for admin operations like creating entities on approval.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Modal API (for execution endpoints)
# Get this by deploying the wheelhouse backend:
#   cd wheelhouse && modal deploy wheelhouse/web/api.py
# Modal will output: https://your-username--wheelhouse-api.modal.run
#
# For local development, run: modal serve wheelhouse/web/api.py
# Then use: http://localhost:8000
MODAL_API_URL=https://your-username--wheelhouse-api.modal.run

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
